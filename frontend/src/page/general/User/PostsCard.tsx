import type { FoodPosts } from "../../../types";


export default function PostsCard({postData} : {postData : FoodPosts[]}) {

    return (
        <div>
            Hello Post Section
        </div>
    )
}