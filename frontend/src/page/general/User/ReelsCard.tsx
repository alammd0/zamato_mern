import type { FoodReels } from "../../../types";

export default function ReelsCard({reelData} : {reelData : FoodReels[]}) {
    return (
        <div>
            Hello Reels Section
        </div>
    )
}